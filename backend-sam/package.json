{
  "name": "slack-integration-backend-sam",
  "version": "1.0.0",
  "description": "Slack連携技術検証用バックエンド (AWS SAM + Node.js 22)",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy",
    "local-start": "sam local start-api",
    "local-invoke": "sam local invoke",
    "logs": "sam logs",
    "validate": "sam validate",
    "sync": "sam sync --watch"
  },
  "devDependencies": {
    "@aws-sam/cli": "^1.118.0"
  }
}
